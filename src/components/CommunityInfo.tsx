interface ICommunityInfoProps {}

const CommunityInfo: React.FC<ICommunityInfoProps> = (props) => {
  return <div className="order-last hidden h-52 w-full bg-primary lg:block" />;
};

export default CommunityInfo;
